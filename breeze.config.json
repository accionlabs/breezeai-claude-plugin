{
  "$schema-doc": "Canonical Breeze service URLs shipped with the plugin. Skills read these as defaults; users can override per-project by setting the same fields in .breeze.json.",
  "uiBaseUrl": "https://ai.accionbreeze.com",
  "apiBase": "https://isometric-backend.accionbreeze.com",
  "_paths": {
    "$doc": "Reference paths (relative to uiBaseUrl) — listed here so skills can reference them by name. Treat as documentation only; SKILL.md files still construct URLs as <uiBaseUrl>/<path>.",
    "apiKeyGenerate": "/mcp/generate/key",
    "codeOntologyUpload": "/code-ontology/<projectUuid>",
    "ontologyGenerate": "/ontology/<projectUuid>/functional"
  }
}
